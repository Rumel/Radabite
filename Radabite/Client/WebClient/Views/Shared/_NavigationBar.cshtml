<div class="contain-to-grid fixed">
    <nav class="top-bar" data-topbar>
        <ul class="title-area">
            <li class="name">
                <h1><a href="#">Radabite</a></h1>
            </li>
            <li class="toggle-topbar menu-icon">
                <a href="#"><span>Menu</span></a>
            </li>
        </ul>

        <section class="top-bar-section">
            <ul class="left">
                <li>@Html.ActionLink("Profile", "UserProfile", new { userId = Model["userId"] })</li>
                <li>@Html.ActionLink("Create", "CreateEvent", new { userId = Model["userId"] })</li>
                <li>@Html.ActionLink("Discover", "DiscoverEvent", new { userId = Model["userId"] })</li>
                
            </ul>
            <ul class="right">
                <li>@Html.ActionLink("Log Off", "")</li>
            </ul>
        </section>
    </nav>
</div>
